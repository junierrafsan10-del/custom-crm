# Custom CRM Backend

## Setup

```bash
npm install
```

## Environment

Copy `.env.example` to `.env` and fill in the values.

## Run

```bash
node server.js
```

The server will start on the port specified in `.env` (default: 5000).
A default admin user is seeded on first run: `admin` / `admin123`
